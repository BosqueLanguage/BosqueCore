namespace Core;

#if STRIPPED_CORE
#else

datatype JSON of 
    JsonNull { }
    JsonBool { field value: Bool; }
    JsonNumber { field value: Float; }
    JsonString { field value: String; }
    JsonArray { field value: List<JSON>; }
    JsonObject { field value: Map<String, JSON>; }
& {
    function toByteBuffer(json: JSON): ByteBuffer {
        abort;
    }

    function fromByteBuffer(buffer: ByteBuffer): Option<JSON> {
        abort;
    }

    function toStdBAPI<T>(json: JSON): Option<T> {
        abort;
    }

    function fromStdBAPI<T>(value: T): JSON {
        abort;
    }
}

#endif %%STRIPPED_CORE
