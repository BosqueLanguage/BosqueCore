namespace Core;

#if STRIPPED_CORE
#else
#if EXEC_LIBS
namespace StringOps {
    function s_empty(s: String): Bool = @string_empty;
    function s_size(s: String): Nat = @string_size;
    function s_bytes(s: String): Nat = @string_bytes;

    function s_append(s1: String, s2: String): String = @string_append;

    function s_toByteBuffer(s: String): ByteBuffer = @string_to_bytebuffer;
    function s_fromByteBuffer(bb: ByteBuffer): Option<String> = @string_from_bytebuffer;
}
#else
#endif %% EXEC_LIBS
#endif %% STRIPPED_CORE