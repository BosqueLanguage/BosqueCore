namespace SMTEmit;

import TreeIR;

export function main(asm: TreeIR::Assembly): String {
    let flatasm = AssemblyEmitter::processAssembly(asm); //TODO should go into tree rewriter

    return AssemblyEmitter::generateSMTCode(flatasm, "", Map<TreeIR::InvokeKey, TreeIR::ErrorKind>{});
}
